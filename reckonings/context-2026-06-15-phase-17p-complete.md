# Context Preservation — 2026-06-15 (Phase 17P complete)

Generated by `/context-preservation` after `/compact`. Pinned to disk so it
survives transcript rotation / fresh-session resumption.

Companion artifacts:
- Conversation transcript: `/Users/jarocki/.claude/projects/-Users-jarocki-src-ap/64f886ed-5370-4530-92ff-ccae2aa1f1c0.jsonl`
- Auto-memory index: `/Users/jarocki/.claude/projects/-Users-jarocki-src-ap/memory/MEMORY.md`
- Code-side authorities: `MASTER_PLAN.md`, `DECISIONS.md`, `reckonings/`

---

### 1. Current Objective & Status

- **Goal**: Drain the AP CTI tool roadmap to a shippable v0.4.x via the canonical
  planner → guardian(provision) → implementer → reviewer → guardian(land) chain.
  The user took the tool "for a spin," reported two real bugs from `ap chat`,
  and asked for both to be closed properly.
  - **Bug 1 (Phase 17O — error routing):** an `httpx.HTTPStatusError` traceback
    leaked through `agent/tools.py::execute_tool` as a stderr stack dump
    instead of a friendly, actionable summary.
  - **Bug 2 (Phase 17P — workspace/db polish):** chat had `workspace` and
    `db_status` commands but no way to list/create/switch/delete/clear
    workspaces from chat, and `db_status` rendered an anemic table.

  Definition of Done:
  - Phase 17O: additive `ErrorInterpreter` catalog matchers + `ToolContext.console`
    wiring; raw tracebacks suppressed.
  - Phase 17P: `WorkspaceManager.clear()` with loud-fail verification + chat
    subcommand parity (list/create/switch/delete/clear) + enhanced `db_status`
    with DB path/size/per-table counts/last-activity ISO timestamps + shared
    `_render_db_status_table()` and `_confirm()` helpers.

  Both phases must land green-tested on `main` without scope creep into forbidden
  authorities (`models/database.py`, `agent/runner.py`, `agent/tools.py` tool
  count, `dossier/**`, `gamification/**`).

- **Status**: Completed. Phase 17O landed at `1ccf13b`. Phase 17P landed at
  `724413a` (merge on `origin/main`, push `474a8a6..724413a`). 28 new tests
  added by 17P pass; full suite `2646 passed, 1 skipped, 4 pre-existing
  failures` (AP #84).

- **Immediate Next Step**: No active dispatch. If the user returns and
  explicitly says "clean up the workspace-db branch" (or error-routing
  equivalent), run:
  ```
  git -C /Users/jarocki/src/ap worktree remove \
    /Users/jarocki/src/ap/.worktrees/feature-workspace-db-2026-06-11
  git -C /Users/jarocki/src/ap branch -d feature/workspace-db-2026-06-11
  ```
  Repeat for `feature-error-routing-2026-06-11`. Otherwise wait for the user's
  next request — the v0.4.x cut (AP #82) is still queued but not yet authorized.

---

### 2. Active Context

- **Active Files** (absolute paths):
  - `/Users/jarocki/src/ap/src/adversary_pursuit/core/workspace.py`
  - `/Users/jarocki/src/ap/src/adversary_pursuit/agent/chat.py`
  - `/Users/jarocki/src/ap/src/adversary_pursuit/core/console.py`
  - `/Users/jarocki/src/ap/src/adversary_pursuit/agent/tools.py`
  - `/Users/jarocki/src/ap/src/adversary_pursuit/core/error_interpreter.py`
  - `/Users/jarocki/src/ap/MASTER_PLAN.md`
  - `/Users/jarocki/src/ap/scripts/regen_decisions.py`
  - `/Users/jarocki/src/ap/DECISIONS.md`
  - `/Users/jarocki/src/ap/pyproject.toml` (version still `0.1.0`; AP #82 will bump to `0.4.0`)

- **Recent Changes**:
  - **Phase 17P merge `724413a`**:
    - `WorkspaceManager.clear(name: str | None = None) -> dict[str, int]` added —
      drops 6 tables (`stix_objects`, `relationships`, `module_runs`,
      `score_events`, `analyst_notes`, `badge_events`) and raises `RuntimeError`
      if any table remains non-empty after clear (Sacred Practice 5).
    - New helpers `get_workspace_db_size`, `get_workspace_table_counts`,
      `get_last_event_timestamps`.
    - `chat.py` `_workspace_cmd` is now a full subcommand dispatcher
      (list/create/switch/delete/clear) with deprecate-and-warn for legacy
      `workspace <name>` shorthand; new `do_db_status` meta-command.
    - `console.py` gained shared `_workspace_clear`, `_confirm` ([y/N]
      default-NO), `_humanise_bytes`, and `_render_db_status_table` helpers;
      `do_db_status` delegates to the shared renderer.
  - **Phase 17O merge `1ccf13b`**:
    - `agent/tools.py::execute_tool` (~lines 2120-2138) replaced
      `logger.exception(...)` with `logger.debug(..., exc_info=True)` so
      tracebacks never hit stderr.
    - Caught exceptions are routed through `core/error_interpreter.py::interpret()`,
      rendered as a Rich panel to `ctx.console` when attached, and returned to
      the LLM as `[USER_SAW_PANEL] {run_fail} {summary}`.
    - `ToolContext.__init__` gained `console: Console | None = None`.
    - `chat.py` wires `runner.ctx.console = console`.
    - `error_interpreter.py` got `_is_httpx_http_status_error` helper + matchers
      for 401/403/429/4xx/5xx inserted BEFORE `_unknown_fallback`
      (DEC-ERROR-ROUTING-002).
  - **Hygiene slice**: landed `scripts/regen_decisions.py` (410 lines,
    stdlib-only, `--dry-run`, self-annotated `DEC-HYGIENE-REGEN-001`) which
    discovered 248 DECs across 7 components and expanded `DECISIONS.md` from
    273 → 1251 lines. Filed because the original `stop.sh`-driven regen
    mechanism was no longer present.
  - **Plan loaded but NOT acted on**:
    `/Users/jarocki/.claude/plans/moonlit-snacking-hejlsberg.md` (F2 — Split
    `CAN_LAND_GIT` into `CAN_COMMIT` + `CAN_PUBLISH`). That work targets the
    **harness repo** (`~/.claude`), not AP. Queued for a separate session.

- **Variables/State**:
  - Current branch: `main` at `724413a`. 7 uncommitted are `.claude/`
    housekeeping artifacts (`.audit-log`, `.lint-cache`, `.session-changes-*`,
    `.prompt-count-*`); none are source.
  - Two worktrees still present and deferred for cleanup:
    - `/Users/jarocki/src/ap/.worktrees/feature-error-routing-2026-06-11`
      (`df88836`, branch `feature/error-routing-2026-06-11`)
    - `/Users/jarocki/src/ap/.worktrees/feature-workspace-db-2026-06-11`
      (`ffc3cd4`, branch `feature/workspace-db-2026-06-11`)
  - Test state: pass. Full suite `2646 passed, 1 skipped, 4 pre-existing
    failures` (AP #84 — fixtures hardcode the removed worktree path
    `/Users/jarocki/src/ap/.worktrees/feature-68-m9-crowdsourced-dossiers`:
    `test_dossier_export.py::TestF59Invariant` ×2 +
    `test_dossier_m9_tools.py::TestF64Invariants` ×2).
  - MASTER_PLAN.md: 38/32 phases done. Active Phase Pointer currently reads
    `### Prior Active Phase Pointer (2026-06-11; preserved for traceability —
    Phase 17O Error Interpreter Routing)` — this is *stale*; Phase 17P should
    be the current pointer. Low priority; fixup pattern is the same
    TBD-sha → real-sha refresh used for C-3/C-4/17O.
  - Open AP issues (5 in HUD + queued release):
    - #70 — Python heredoc bypass detection
    - #71 — worktree provision parent HEAD
    - #74 — M-1/C-1 cross-refs
    - #75 — work_item auto-transition to landed
    - #76 — untracked carry-forward audit
    - #82 — v0.4.x cut (bump pyproject, CHANGELOG, tag, PyPI)
    - #83 — bulk-scoped approval grants
    - #84 — test fixtures hardcode removed worktree path
    - #72 (DECISIONS.md regen) closed via hygiene slice merge `3cf14a7`.

---

### 3. Constraints & Decisions (CRITICAL)

- **Preferences**:
  - Direct user quote: "Don't make anything up. Use only facts. Build only
    excellent code that gets the work done, resiliently and securely, with the
    minimal codebase (ie, as simple as possible, but no simpler)."
  - User errors must be summarized at a high level, NOT shown as raw
    tracebacks, AND must offer a concrete fix proposal in the form
    "I can fix this by doing X. Would you like me to proceed with X?"
  - Stop Advisor reinforced repeatedly: do NOT ask user for routine canonical
    dispatch, do NOT ask permission on bookkeeping, but DO file backlog items
    for discovered issues.

- **Discarded Approaches**:
  - **Worktree Python 3.14 venvs for running tests** — rejected because
    they're missing optional deps (`prompt_toolkit`, `litellm`) and produce
    false "16 pre-existing failures" claims. Mandatory pattern:
    `(cd /Users/jarocki/src/ap && PYTHONPATH=<worktree>/src uv run pytest <worktree>/tests/...)`
    using the AP-root Python 3.13 venv.
  - **Single big monolithic v0.4 PR** — rejected in favor of the
    M-/C-/dossier slice cadence with explicit Evaluation Contracts and
    AP #74 orphan-prevention.
  - **Letting implementer pre-compute its own commit SHA** — rejected
    (chicken/egg). Pattern is always: implementer commits with `TBD-sha`
    placeholders in MASTER_PLAN.md, then a small docs-only fixup commit
    refreshes them to the real SHA. Used for C-3 (`cb4c937`), C-4
    (`4443f50`), Phase 17O (`df88836`), Phase 17P (`ffc3cd4`).
  - **Mocking the SQLite workspace DB in tests** — never. Real
    `WorkspaceManager` against a tmp path; verify post-clear emptiness with
    actual SELECT counts.
  - **Treating `logger.exception()` as user-facing** — never. It dumps to
    stderr and bypasses Rich. Always use `logger.debug(..., exc_info=True)`
    for traces and route the user-visible summary through `ErrorInterpreter`.

- **Architectural Rules**:
  - **Sacred Practice 12 — Single Source of Truth.** cmd2 console and chat
    MUST share the same `_render_db_status_table()`, `_confirm()`, and
    `_workspace_clear()` helpers. Never two implementations of one concept.
  - **Sacred Practice 5 — Loud Failure.** `WorkspaceManager.clear()` raises
    `RuntimeError` if post-clear verification finds any non-empty table.
  - **Sacred Practice 2 — Main is Sacred.** All source work happens in
    worktrees; orchestrator only edits docs/config qualifying for the Simple
    Task Fast Path.
  - **Forbidden invariants preserved across all recent slices:**
    `models/database.py` schema unchanged, `agent/runner.py` not touched,
    `agent/tools.py` tool count stays at 30, `dossier/**` byte-identical for
    M-1..M-9, `gamification/**` byte-identical, `core/error_interpreter.py`
    extension is additive-only (matchers inserted BEFORE `_unknown_fallback`,
    zero rewrites).
  - **Confirmation prompts** use `[y/N]` with default-NO at every destructive
    UI surface (DEC-WORKSPACE-DB-001/006).
  - **DEC- annotations** are mandatory at the point of implementation for any
    non-trivial decision; `scripts/regen_decisions.py` keeps `DECISIONS.md`
    in sync.
  - **Bulk Guardian operations** (worktree removal + branch delete) need
    pre-loop of single-shot approval grants per call; do NOT call them under
    one umbrella grant (filed as AP #83).

---

### 4. Continuity Handoff

When resuming, the first thing to do is read
`/Users/jarocki/src/ap/MASTER_PLAN.md` (Plan Status table + Active Phase Pointer
at bottom) to confirm Phase 17P (`724413a`) is reflected, then ask the user
which they want next:

- **(a) Cleanup** of the two deferred worktrees
  (`feature-error-routing-2026-06-11` + `feature-workspace-db-2026-06-11`).
- **(b) v0.4.x cut** (AP #82 — bump `pyproject.toml` `version = "0.1.0"` →
  `"0.4.0"`, update `CHANGELOG.md`, tag, dispatch through the canonical chain).
- **(c) AP #84 fixture cleanup** (4 hardcoded
  `/Users/jarocki/src/ap/.worktrees/feature-68-m9-crowdsourced-dossiers` paths
  in `tests/test_dossier_export.py::TestF59Invariant` +
  `tests/test_dossier_m9_tools.py::TestF64Invariants`).
- **(d) F2 capability split plan** in `~/.claude` (different repo entirely,
  plan at `/Users/jarocki/.claude/plans/moonlit-snacking-hejlsberg.md`).

Do NOT auto-start any of these — Phase 17P closed the user's last explicit
ask and there is no live canonical dispatch waiting. Wait for the user's pick
before invoking `cc-policy workflow stage-packet`.
